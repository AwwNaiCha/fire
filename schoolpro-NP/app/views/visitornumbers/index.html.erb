<h1>Listing National Parks</h1>

<table>
  <thead>
    <tr>
      <th>Park Name</th>
      <th>Park Name</th>
      <!--th>Jan</th -->
      <!--th>Feb</th -->
      <!-- th>Mar</th -->
      <!-- th>Apr</th -->
      <!-- th>May</th -->
      <!-- th>Jun</th -->
      <!-- th>Jul</th -->
      <!-- th>Aug</th -->
      <!-- th>Sep</th -->
      <!-- th>Oct</th -->
      <!-- th>Nov</th -->
      <!-- th>Dec</th -->
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @visitornumbers.each do |visitornumber| %>
      <tr>
        <td><%= visitornumber.park_name %></td>
        <!-- td><%= visitornumber.jan %></td -->
        <!-- td><%= visitornumber.feb %></td -->
        <!-- td><%= visitornumber.mar %></td -->
        <!-- td><%= visitornumber.apr %></td -->
        <!-- td><%= visitornumber.may %></td -->
        <!-- td><%= visitornumber.jun %></td -->
        <!-- td><%= visitornumber.jul %></td -->
        <!-- td><%= visitornumber.aug %></td -->
        <!-- td><%= visitornumber.sep %></td -->
        <!-- td><%= visitornumber.oct %></td>
        <td><%= visitornumber.nov %></td>
        <td><%= visitornumber.dec %></td -->
        <td><%= link_to 'Show', visitornumber %></td>
        <% if user_signed_in? && current_user.admin %>
        <td><%= link_to 'Edit', edit_visitornumber_path(visitornumber) %></td>
        <td><%= link_to 'Destroy', visitornumber, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<% if user_signed_in? && current_user.admin %>
<%= link_to 'New Visitornumber', new_visitornumber_path %>
<%end%>