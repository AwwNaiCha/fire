<%= form_for(@fire_level) do |f| %>
  <% if @fire_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fire_level.errors.count, "error") %> prohibited this fire_level from being saved:</h2>

      <ul>
      <% @fire_level.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :park_name %><br>
    <%= f.text_field :park_name %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :level %><br>
    <%= f.text_field :level %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
